/**
 * here we can export the extra
 * plugins that the app need. so
 * we do not need to import them.
 */
import dayJs from 'dayjs'
import * as echarts from 'echarts'

export {
  dayJs,
  echarts,
}
